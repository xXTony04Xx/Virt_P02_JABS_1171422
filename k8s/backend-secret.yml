# Secret para las variables de entorno del backend
# IMPORTANTE: reemplaza los valores con tus datos reales en base64
# Para codificar: echo -n "tu_valor" | base64
 
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  MONGO_URI: "mongodb://mongo-service:27017/salon_db"
  JWT_SECRET: "mi_clave_secreta_salon"
  FLASK_PORT: "5000"
